<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>打开微信，扫描下面这个二维码：</title>
    <style>
        .lds-ripple {
            display: inline-block;
            position: relative;
            width: 64px;
            height: 64px;
        }

        .lds-ripple div {
            position: absolute;
            border: 4px solid rgb(0, 0, 0);
            opacity: 1;
            border-radius: 50%;
            animation: lds-ripple 1s cubic-bezier(0, 0.2, 0.8, 1) infinite;
        }

        .lds-ripple div:nth-child(2) {
            animation-delay: -0.5s;
        }

        @keyframes lds-ripple {
            0% {
                top: 28px;
                left: 28px;
                width: 0;
                height: 0;
                opacity: 1;
            }
            100% {
                top: -1px;
                left: -1px;
                width: 58px;
                height: 58px;
                opacity: 0;
            }
        }
    </style>
</head>

<body>
    <h1>登陆</h1><div class="lds-ripple">
            <div></div>
            <div></div>
        </div>
    <p>等待扫描微信二维码。</p>
    <p>完成扫描后等待一两秒钟将自动进入console</p>
    <hr>
    <img src="/getCode">
    <br>
    
    <p>用微信扫描二维码来登陆</p>
</body>
<script src="/socket.io/socket.io.js"></script>
<script src="https://code.jquery.com/jquery-1.11.1.js"></script>
<script>
    $(function () {
        console.log('123')
        var socket = io()
        socket.on('loginSuccessed', function () {
            console.log('sss')
            self.location.href = "/action/scaned"
        })
    })
</script>

</html>